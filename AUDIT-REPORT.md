# דוח בדיקה מקיף — אתר La Shirel

> נערך: יוני 2026 · אתר `index.html` (סטטי, GitHub Pages) · מותג בגדי ים La Shirel
> סימוני דחיפות: 🔴 קריטי · 🟠 חשוב · 🟢 שיפור

---

## תמונת מצב כללית

אתר עמוד-יחיד (`index.html`, ~1,400 שורות, כל ה-CSS וה-JS מוטמעים בקובץ), סטטי, מתארח על **GitHub Pages**. הקנייה מתבצעת דרך קישור WhatsApp (אין סליקה אמיתית). 30 מוצרים מקודדים קשיח ב-JavaScript.

**הרושם הראשוני מצוין** — עיצוב נקי, יוקרתי, רספונסיבי, עם תשומת לב לפרטים (אנימציות, גלריית תמונות, עגלה, מודלים משפטיים). זה כבר בסיס חזק. עיקר ההמלצות הן ליטוש, סגירת חשיפות משפטיות, וביצועים.

---

## 🔴 1. חשיפות משפטיות (הכי חשוב — שלא יהיה מקום לתביעות)

זה התחום שבו נמצאו הסיכונים האמיתיים.

### א. סתירה בין ה-marquee למדיניות ההחזרות
בפס הנע למעלה כתוב: *"אין החלפות והחזרות על בגדי ים"* — הצהרה גורפת. אבל בתקנון עצמו כתוב שעל **בגד ים לגברים ניתן להחזיר תוך 7 ימים**. סתירה כזו בעייתית: לפי חוק הגנת הצרכן, שלט גורף של "אין החזרות" עלול להיחשב **תנאי מקפח/מטעה**. צריך ליישר קו — לרכך את ה-marquee (למשל "מדיניות החזרות מיוחדת לבגדי ים — ראו תקנון").

### ב. הצהרת הנגישות מצהירה על עמידה ב-WCAG 2.1 AA — אך האתר לא באמת עומד בזה
ההצהרה כותבת מפורשות *"ניגודיות צבעים עומדת בדרישות WCAG AA"* ו*"ניווט מקלדת מלא"*. בפועל (ראה סעיף נגישות) — **הניגודיות נכשלת** בחלקים רחבים, ותמונות המוצרים בגריד אינן נגישות כלל. **הצהרת נגישות כוזבת היא עצמה עילה לתלונה/תביעה** לפי תקנות שוויון זכויות. שתי אפשרויות: לתקן את האתר כך שיעמוד בהצהרה, או לרכך את ההצהרה ל"אנו שואפים ומשפרים באופן שוטף" + לפרט רק מה שבוצע בפועל. מומלץ גם להוסיף **שם רכז/ת נגישות** וכתובת מייל ייעודית (לא רק WhatsApp).

### ג. פרטי עוסק חסרים — חובה בעסקה מרחוק
לפי חוק הגנת הצרכן (עסקת מכר מרחוק), חובה לפרסם: **שם העוסק המלא, מספר ע.מ./ח.פ., וכתובת**. כרגע אין באתר שום מספר עוסק, כתובת או מייל פורמלי — רק WhatsApp ואינסטגרם. זו חשיפה ממשית. מומלץ להוסיף בתקנון/פוטר את הפרטים האלה.

### ד. מדיניות הפרטיות מתארת איסוף שכבר לא קיים
המדיניות אומרת *"כתובות מייל — אם נרשמת לניוזלטר"*, אבל **טופס הניוזלטר הוסר** מהאתר (הוחלף בקישור לקבוצת WhatsApp). יש קוד מת של הטופס. מדיניות שמתארת איסוף נתונים שלא מתבצע = לא מדויק. יש ליישר: או להחזיר טופס, או לעדכן את המדיניות שהאיסוף הוא דרך WhatsApp (ואז להזכיר ש-Meta/WhatsApp הם מעבדי המידע).

### ה. תיקון 13 לחוק הגנת הפרטיות
נכנס לתוקף ב-08/2025 והחמיר דרישות. שווה התאמה קצרה של המדיניות (בסיס חוקי לעיבוד, זכויות מורחבות).

---

## 🟠 2. אבטחה

**הצד החיובי — מרשים:** יש CSP הדוק (גם ב-`_headers` וגם ב-`<meta>`), `X-Frame-Options`, `nosniff`, `Referrer-Policy`, `Permissions-Policy`, ו-`rel="noopener noreferrer"` על כל הקישורים החיצוניים. הרבה מעבר למקובל באתרים קטנים.

### הבעיה המרכזית: קובץ `_headers` לא עובד ב-GitHub Pages 🔴
הפורמט הזה שייך ל-Netlify/Cloudflare Pages. GitHub Pages (שם האתר מתארח, לפי ה-workflow) **מתעלם ממנו לחלוטין**. כלומר ה-headers שאמורים להגן — `X-Frame-Options`, `Permissions-Policy`, `X-Content-Type-Options` — **לא נשלחים בפועל**. רק ה-CSP וה-referrer שב-`<meta>` עובדים (ו-`X-Frame-Options` דרך meta לא נתמך בדפדפנים — אין הגנת clickjacking אמיתית).

**שתי דרכים לפתור:**
1. מעבר לאירוח שתומך ב-headers (Cloudflare Pages / Netlify / Vercel — חינמי). ב-Vercel הגדרת headers דרך `vercel.json`.
2. להישאר ב-GitHub Pages ולקחת בחשבון את המגבלה.

**הערות נוספות (סיכון נמוך):**
- ה-CSP מתיר `'unsafe-inline'` ל-script — נחלש בגלל שכל הקוד inline. כרגע אין קלט משתמש שמרונדר, אז ה-XSS בפועל נמוך, אבל זו נקודת תורפה עקרונית.
- שימוש נרחב ב-`innerHTML` עם נתוני מוצר. כל הנתונים קשיחים ובטוחים כיום — אבל אם אי-פעם יגיע נתון ממקור חיצוני, זו פרצת XSS. דפוס לשים לב אליו.
- אין סודות/מפתחות בריפו. טוב.

---

## 🟠 3. באגים פונקציונליים

1. **קטגוריית "בגדי חוף" ריקה לגמרי** 🔴 — יש כפתור סינון "בגדי חוף", קישור "Beachwear" בתפריט, וקישור בפוטר — אבל **0 מוצרים** מוגדרים עם `cat:"beach"`. לחיצה מציגה גריד ריק לבן. הבטחה שבורה למשתמש. צריך לאכלס את הקטגוריה, או להסיר את הכפתור והקישורים.
2. **שם מוצר כפול "ריביירה"** — מופיע פעמיים (פריט נשים פסים כחול-לבן + סט זוגי פסים ירוק-לבן). מבלבל. כדאי שם ייחודי לאחד.
3. **העגלה לא נשמרת ברענון דף** — שמורה רק בזיכרון. משתמשת שתרענן/תחזור מאבדת את הסל. תיקון פשוט: שמירה ב-`localStorage`.
4. **קוד מת** — `.news-form` (CSS) ופונקציית `joinNews()` נשארו אך הטופס הוסר מה-HTML. ניקוי.
5. **חוסר עקביות במזהי sections** — הגלריה "מצאי את הסגנון שלך" קיבלה `id="couples"` (לא נכון סמנטית). מינורי.

---

## 🟠 4. נגישות (פירוט)

**קיים וטוב:** `sr-only`, `focus-visible`, `aria-label` על כפתורים, `role="button"`+תמיכת מקלדת על הכרטיסים, `prefers-reduced-motion`, מבנה כותרות סביר.

**מה חסר/שגוי:**
- **תמונות המוצרים בגריד אינן נגישות** 🔴 — מוצגות כ-`background-image` ב-CSS (`.ph`), בלי `alt`. קורא מסך לא "רואה" אותן. רק במודל יש `<img alt>` אמיתי. פגיעה מהותית בנגישות (וגם ב-SEO). מומלץ להחליף ל-`<img>` אמיתי עם `alt`.
- **ניגודיות נכשלת** 🔴 — צבע הטקסט המשני `--soft` (#8C857B) על רקע `--bg` (#FBFAF7) נותן יחס של ~3.3:1, **מתחת לסף 4.5:1** הנדרש לטקסט רגיל. הצבע משמש בתיאורים, מחירים מוזלים, פוטר, ותגיות. גם הכתום `--accent` על לבן בטקסטים קטנים גבולי. צריך להכהות גוונים אלה.
- **אין מלכודת פוקוס (focus trap) במודלים ובמגירות** — משתמש מקלדת יכול לטאב "מאחורי" המודל הפתוח. צריך גם להחזיר פוקוס לכפתור שפתח, ולהוסיף `aria-modal` למודל המוצר (קיים רק במשפטי).
- **אין קישור "דלג לתוכן"** (skip link).
- **`prefers-reduced-motion`** מבטל רק את ה-marquee — אך יש עוד הרבה אנימציות (חשיפת hero, fade של כרטיסים, scale ב-hover). ההצהרה מרמזת על תמיכה רחבה יותר.

---

## 🟢 5. ביצועים

- **37MB של תמונות** בריפו, ללא אופטימיזציה, ללא `loading="lazy"`, ללא `width/height` (גורם ל-CLS — קפיצת תוכן). הדבר המשמעותי ביותר לחוויה במובייל. המלצות: המרה ל-**WebP/AVIF**, הקטנת רזולוציה, הוספת `loading="lazy"` ו-`width/height`/`aspect-ratio`.
- **וידאו hero 3.4MB** עם autoplay — מומלץ `poster` (תמונה ראשונה) + טעינה דחויה, וגרסה קלה למובייל.
- **גופני Google חיצוניים** חוסמים רינדור. מעבר ל**אירוח עצמי של הגופנים** מרוויח שלושה דברים בבת אחת: ביצועים, ביטול חשיפת ה-IP לגוגל (ואז אפשר להסיר את באנר העוגיות לגמרי!), ו-CSP הדוק יותר.

---

## 🟢 6. SEO ושיתוף ברשתות

חסרים לחלוטין ושווים הרבה לעסק שמשתף באינסטגרם/וואטסאפ:
- **אין Open Graph / Twitter Card** — כשמשתפים את הקישור ב-WhatsApp/אינסטגרם, לא תופיע תמונה/כותרת יפה. הוספת `og:title`, `og:description`, `og:image` היא win גדול.
- אין `favicon`, `apple-touch-icon`, `manifest` (PWA).
- אין JSON-LD (Structured Data) למוצרים/ארגון — עוזר לגוגל.
- אין `robots.txt`, `sitemap.xml`, `canonical`.
- אין דף 404.

---

## 🟢 7. ממשק וטיפוגרפיה (כולל רמת הפונטים)

1. **Cormorant Garamond אין בו אותיות עבריות** 🔴 (טיפוגרפית) — הגופן הזה (`--serif`) לטיני בלבד. אך מוחל על כותרות **עבריות** כמו "מצאי את הסגנון שלך", "הקולקציה המלאה". התוצאה: העברית נופלת ל**גופן ברירת מחדל של הדפדפן** (דויד/פרנק רוהל מערכתי), שמתנגש עם האסתטיקה היוקרתית. **פתרון:** לזווג גופן serif עברי איכותי כמו **Frank Ruhl Libre** לכותרות בעברית, ולהשאיר את Cormorant ללטינית בלבד.

2. **letter-spacing על טקסט עברי** 🟠 — ברחבי האתר יש `letter-spacing` של ‎.14em עד ‎.4em על kickers, ניווט וכפתורים. ריווח-אותיות **שגוי טיפוגרפית בעברית** (האותיות לא אמורות להתרווח) ופוגע בקריאות. בלטינית זה יפה — בעברית צריך לאפס. כדאי להחיל tracking רק על מחרוזות לטיניות.

3. **משקל גופן 300 (דק) לטקסט גוף** — Heebo במשקל 300, קטן ובצבע בהיר, קשה לקריאה. מומלץ 400 לגוף ולשמור 300 לכותרות גדולות בלבד.

4. **גדלי טקסט קטנים** — 10–12px בתגיות, meta, פוטר, באנר עוגיות. למובייל ולנגישות מומלץ מינימום 12–14px.

5. **ערבוב עברית/אנגלית בניווט** — "Home / Women / Men" באנגלית בתוך אתר עברי `dir="rtl"`. לגיטימי כבחירת מותג, אך כדאי החלטה עקבית.

---

## סיכום — סדר עדיפויות מומלץ

| עדיפות | פעולה |
|--------|-------|
| 🔴 משפטי | יישור marquee↔תקנון; ריכוך/תיקון הצהרת נגישות; הוספת פרטי עוסק (ח.פ./ע.מ.+כתובת); יישור מדיניות פרטיות למצב בפועל |
| 🔴 פונקציונלי | תיקון קטגוריית "בגדי חוף" הריקה |
| 🔴 אבטחה | החלטה לגבי `_headers` שלא עובד ב-GitHub Pages |
| 🔴 נגישות | תמונות מוצר נגישות (`<img alt>`) + תיקון ניגודיות |
| 🟠 | שמירת עגלה ב-localStorage; גופן עברי לכותרות; הסרת letter-spacing מעברית; focus-trap במודלים |
| 🟢 | אופטימיזציית תמונות/וידאו; OG tags+favicon; ניקוי קוד מת; שם ייחודי ל"ריביירה" הכפול |

---

*דוח זה הוא המלצות בלבד — לא בוצעו שינויים בקוד האתר. ניתן ליישם כל חלק בנפרד.*
